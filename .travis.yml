os:
    - linux
    - osx

addons:
    apt:
        packages: &common-apt-packages
            - vim

    homebrew:
        update: false
        packages:
            - vim

before-script: make simple
script: test/test.sh
