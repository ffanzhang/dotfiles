os:
    - linux
    - osx
    - windows

addons:
    apt:
        update: false
        packages: &common-apt-packages
            - vim

    homebrew:
        update: false
        packages:
            - vim

    choco:
        update: false
        packages:
            - vim

before-script: make kiss
    case $TRAVIS_OS_NAME in
      windows)
        install.bat;
        ;;
      linux)
        make kiss;
        ;;
      osx)
        make kiss;
        ;;
    esac

script: 
    test/test.sh
